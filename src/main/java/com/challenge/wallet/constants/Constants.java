package com.challenge.wallet.constants;

import java.math.BigDecimal;

public class Constants {

    public static BigDecimal ONE_CENT = BigDecimal.valueOf(0.01);

    private Constants() {
        throw new IllegalStateException();
    }
}